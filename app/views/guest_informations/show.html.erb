<div class="container-pref-crew">
    <h1 class="guest-info-name"><%= "#{@guest_information.user.name} information" %></h1>
    <div class="guest-info-info">
        <p><%= "birth date :#{@guest_information.birth_date}" %></p>
        <p><%= "passport number :#{@guest_information.passport_number}" %></p>
        <p><%= "nationality :#{@guest_information.nationality}" %></p>
        <p><%= "mobile number :#{@guest_information.mobile_number}" %></p>
        <p><%= "arrival date :#{@guest_information.arrival_date}" %></p>
        <p><%= "departure date :#{@guest_information.departure_date}" %></p>

        <% if @guest_information.collecting_at_airport == true %>
            <p><%= "#{@guest_information.user.name} want to be collected at the airport, flight number : #{@guest_information.flight_number}" %></p>
        <% else %>
            <p><%= "#{@guest_information.user.name} did not tell us if they want to be picked at the airport" %></p>
        <% end %>

        <% if @guest_information.in_hotel == true %>
            <p><%= "#{@guest_information.user.name} is in the hotel : #{@guest_information.hotel_name}" %></p>
        <% else %>
            <p><%= "#{@guest_information.user.name} did not tell us if he is staying in a hotel" %></p>
        <% end %>
    </div>
</div>